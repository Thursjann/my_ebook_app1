import 'package:flutter/material.dart';

class HelpCenterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('รายการที่ชอบ')),
      body: Center(child: Text('หน้านี้คือ รายการที่ชอบ')),
    );
  }
}